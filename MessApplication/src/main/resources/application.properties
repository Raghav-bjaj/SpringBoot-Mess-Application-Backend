# Application settings
spring.application.name=MessSecond
server.port=${PORT:8080}

# Database configuration (Aiven MySQL)
spring.datasource.url=${DATABASE_URL:jdbc:mysql://messdatabase-raghavbajaj1702-96ce.j.aivencloud.com:13795/defaultdb?ssl-mode=REQUIRED}
spring.datasource.username=${DB_USERNAME:avnadmin}
spring.datasource.password=${DB_PASSWORD:AVNS_lHwW6qrNHUneWrhbxi6}
spring.datasource.driver-class-name=${DB_DRIVER:com.mysql.cj.jdbc.Driver}

# Hibernate configuration
spring.jpa.hibernate.ddl-auto=${JPA_DDL_AUTO:validate}
spring.jpa.show-sql=${JPA_SHOW_SQL:false}
spring.jpa.database-platform=${JPA_DIALECT:org.hibernate.dialect.MySQLDialect}

# HikariCP settings for Aiven
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.connection-timeout=30000
